import styled from "styled-components";

export const HeaderWrapper = styled.div`
  position: sticky;
  top: 0;
  z-index: 10;
  padding: 25px 20px;

  .header-right {
    display: flex;
    align-items: center;
    gap: 20px;
  }
`;
